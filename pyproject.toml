[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"

[project]
name = "ibx"
version = "0.7.1"
requires-python = ">=3.11"

[tool.maturin]
features = ["python", "extension-module"]
