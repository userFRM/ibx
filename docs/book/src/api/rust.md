# Rust API

Two shapes reach the same session. Neither is a wrapper around the other in the
sense of a second implementation — they are two surfaces on one engine.

| Type | What it is |
| --- | --- |
| `ibx::Client` | The one to reach for in new code. A call sends the question and hands back the answer, and what the session already holds — a position, an order, a fill, a quote — is read rather than asked for |
| `ibx::EClient` / `ibx::Wrapper` | The reference client's shape: a request under an id, and an answer later on a callback you implement. For a program being moved onto this |
| `ibx::AsyncClient` | `Client` for a program already inside a runtime. Behind the `async` feature |
| `ibx::Config` | What a session is opened with. The same type `EClient` takes |
| `ibx::PlacedOrder` | An order that has been placed, and what is becoming of it |

`Contract` and `Order` are at `ibx::api::client::{Contract, Order}`.

## Building the reference

The crate is not published to an index, so there is no docs.rs page for it.
Build the reference from the source:

```bash
cargo doc --no-deps --open
```

That is the full API, not a summary. The crate carries `#![deny(missing_docs)]`,
so a public item with nothing written on it fails the build rather than shipping
bare, and CI builds the documentation with `-D warnings`, so an intra-doc link
that stops resolving fails too.

## Snapshot of this release

Below is a flat snapshot of the same surface — grouped by area, with signatures
and field tables — for reading without a checkout. It is generated by
`scripts/gen_api_docs.py` from the source; CI regenerates it on every push and
fails if what is committed has drifted.

{{#include ./rust-reference.md}}
